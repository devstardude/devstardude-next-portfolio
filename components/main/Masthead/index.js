import React from 'react';
import Heading from './Heading';

const Masthead = (props)=>{
    return(
    <React.Fragment>
        <Heading/>
    </React.Fragment>
);
};

export default Masthead ;
